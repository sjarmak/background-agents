{
  "mcpServers": {
    "sourcegraph": {
      "command": "npx",
      "args": ["-y", "@sourcegraph/mcp-server"],
      "env": {
        "SRC_ACCESS_TOKEN": "${SRC_ACCESS_TOKEN}",
        "SOURCEGRAPH_URL": "${SOURCEGRAPH_URL}"
      }
    }
  }
}
