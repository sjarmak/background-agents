{
  "mcpServers": {
    "sourcegraph": {
      "command": "npx",
      "args": ["-y", "@nicepkg/sourcegraph-mcp@1.3.0"],
      "env": {
        "SRC_ENDPOINT": "${SOURCEGRAPH_ENDPOINT}",
        "SRC_ACCESS_TOKEN": "${SOURCEGRAPH_TOKEN}"
      }
    }
  }
}
